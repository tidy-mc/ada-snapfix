# Worker Configuration
WORKER_SCAN_URL=http://localhost:9999/api/scan
WORKER_SCAN_SYNC_URL=http://localhost:9999/api/scan-sync

# Optional: External browser service (if using separate browser service)
# PLAYWRIGHT_BROWSER_WS_ENDPOINT=wss://your-worker-vps.com/browser

# For production, update these URLs to your worker VPS:
# WORKER_SCAN_URL=https://your-worker-vps.com/api/scan
# WORKER_SCAN_SYNC_URL=https://your-worker-vps.com/api/scan-sync
